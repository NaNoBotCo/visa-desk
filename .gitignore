# Partner list, QR codes and stickers. Real people; stays on this machine.
partners-private/

# The names that must not appear on the site. Local only — putting it in the
# repo would publish the very thing it guards. tools/check.py reads it if it
# is there and is quiet if it is not.
.names-not-on-the-site

.DS_Store
.wrangler/
node_modules/
*.tmp.svg

# Local operating notes: machine paths and the desk's own working detail.
README.txt

# Worker secrets: real recipient addresses and API keys.
worker/.dev.vars

# Generated commission statements.
statements/

.dev.vars*
!.dev.vars.example
.env*
!.env.example

# White-label register: real licensees.
licences-private/

# Touches the Cloudflare account; stays on this machine.
tools/finish_email.py
Finish email.command
